% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotated_scatterplot.R
\name{annotated_scatterplot}
\alias{annotated_scatterplot}
\title{Annotated scatterplot for teaching association and regression}
\usage{
annotated_scatterplot(
  x,
  y,
  na.rm = TRUE,
  add_lm = TRUE,
  add_correlation = TRUE,
  correlation_method = c("pearson", "spearman"),
  highlight_n = 0L,
  point_labels = NULL,
  point_col = "#1F77B4",
  point_pch = 19,
  line_col = "#D55E00",
  main = NULL,
  xlab = NULL,
  ylab = NULL,
  label_digits = 2,
  legend_position = "topleft",
  ...
)
}
\arguments{
\item{x}{Numeric vector shown on the x-axis.}

\item{y}{Numeric vector shown on the y-axis.}

\item{na.rm}{Logical, should incomplete pairs be removed?}

\item{add_lm}{Logical, should an OLS line be added when possible?}

\item{add_correlation}{Logical, should a correlation coefficient be shown in
a legend?}

\item{correlation_method}{Correlation method passed to \code{stats::cor()}.}

\item{highlight_n}{Number of observations to label. When a regression line is
available, the observations with the largest absolute standardized
residuals are selected.}

\item{point_labels}{Optional labels for the observations.}

\item{point_col}{Colour of the points.}

\item{point_pch}{Plotting symbol for the points.}

\item{line_col}{Colour of the regression line.}

\item{main}{Main title. When \code{NULL}, a default title is used.}

\item{xlab}{Label for the x-axis. When \code{NULL}, the expression supplied
to \code{x} is used.}

\item{ylab}{Label for the y-axis. When \code{NULL}, the expression supplied
to \code{y} is used.}

\item{label_digits}{Number of digits used for the legend annotation.}

\item{legend_position}{Position passed to \code{graphics::legend()}.}

\item{...}{Additional arguments passed to \code{graphics::plot()}.}
}
\value{
Invisibly returns a list containing the plotted data, the correlation
  coefficient, and regression summary information when available.
}
\description{
Draw a scatterplot with optional correlation and OLS annotations
}
\details{
This helper is meant for teaching relationships between two continuous
variables. It can overlay an ordinary least squares line, report a
correlation coefficient, and optionally label a small number of unusual
observations so students can discuss influence and fit.

The function stays intentionally lightweight. It does not try to replace a
full plotting package; instead it provides a consistent base R visual that is
easy to reuse in tutorials and class demonstrations.
}
\examples{
x <- stats::rnorm(120)
y <- 2 + 0.8 * x + stats::rnorm(120, sd = 0.6)

bootcamp::annotated_scatterplot(x, y)
bootcamp::annotated_scatterplot(x, y, highlight_n = 2L)
}
